<%= form_for(@geocoded_location) do |f| %>
  <% if @geocoded_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@geocoded_location.errors.count, "error") %> prohibited this geocoded_location from being saved:</h2>

      <ul>
      <% @geocoded_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_field :address %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
